#!/bin/bash

# ==============================================================================
# GSIRW - OPTIMIZED & RESTORED INTERFACE
# ==============================================================================

# --- Variables Globales y Configuración Visual ---

RED='\033[0;31m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Variable oculta para redirigir outputs molestos
hider="/dev/null"
[ "$DEBUG" = "true" ] && hider="/dev/stdout"

export LOGO='
----------------------------------------------------
|           ____ ____ ___                          |
|         / ___/ ___|_ _|     _ ____      __       |
|        | |  _\___ \| |_____|  __\ \ /\ / /       |
|        | |_| |___) | |_____| |   \ V  V /        |
|         \____|____/___|    |_|    \_/\_/         |
|                 _     ___    ___                 |
|                /_\   |_ _|  / _ \                |
|               / _ \   | |  | (_) |               |
|              /_/ \_\ |___|  \___/                |
|                                                  |
|           Made by: TheDarkDeath788               |
|                                                  |
|   If u get any bug, im not responsable, you can  |
|      try to make a issue on "e2fsprogs" repo     |
----------------------------------------------------
'

# Decodificación segura de LOGOV2 (evitando errores si base64 falla)
if command -v base64 >/dev/null; then
    LOGOV2="$(echo "CiguIFkgLikKKSAgICgKKCAgICAgKQo==" | base64 -d 2>/dev/null)"
else
    LOGOV2="(  Y  )\n)   (\n(     )"
fi

# --- Funciones de Utilidad (Optimizadas) ---

function generate_date_message() {
    # Bash puro (más rápido que llamar a date)
    printf -v hour "%(%Hhs:%Mmin)T" -1
    printf -v date_str "%(%dday|%mmon|%Yyear)T" -1
    day="[$hour]($date_str)"
}

function clear_screen() {
    printf '\033c'
}

# Obtener ancho de terminal de forma compatible
function get_term_width() {
    local cols=$(tput cols 2>/dev/null)
    if [ -z "$cols" ]; then
        cols=$(stty size 2>/dev/null | awk '{print $2}')
    fi
    echo "${cols:-80}"
}

function center_ansi() {
    local text="$1"
    local width=$(get_term_width)
    echo "$text" | awk -v w="$width" '{
        len = length($0)
        if (len < w) padding = int((w - len) / 2)
        else padding = 0
        printf "%*s%s\n", padding, "", $0
    }'
}

function print_justified() {
    local text="$1"
    local width=$(get_term_width)
    local len=${#text}
    local padding=$(( (width - len) / 2 ))
    [ $padding -lt 0 ] && padding=0
    printf "%${padding}s%s\n" "" "$text"
}

# Animación de escritura progresiva (Optimizada)
function progressive_print() {
    local text="$1"
    local delay="${2:-0.05}"
    
    # Si la animación está desactivada, imprimir de una vez
    if [ "$ANIMATION" != "true" ]; then
        echo "$text"
        return
    fi

    local len=${#text}
    for (( i=0; i<len; i++ )); do
        printf "%s" "${text:$i:1}"
        sleep "$delay"
    done
    echo ""
}

function sleepy() {
    [ "$ANIMATION" = "true" ] && sleep "$1"
}

function ask() {
    print_justified "Press any key to continue..."
    read -rsn1
}

# --- Configuración Core ---

function default() {
    DEFAULT_SYSTEM_NAME="system"
    DEFAULT_VENDOR_NAME="vendor"
    DEFAULT_PRODUCT_NAME="product"
    DEFAULT_ODM_NAME="odm"
    
    # Valores por defecto si no existen
    : "${CUSTOM_SYSTEM_NAME:=system}"
    : "${CUSTOM_VENDOR_NAME:=vendor}"
    : "${CUSTOM_PRODUCT_NAME:=product}"
    : "${CUSTOM_ODM_NAME:=odm}"
    
    : "${CUSTOM_MOUNT_POINT:=/cache/system}"
    : "${DEFAULT_START_POINT:=/sdcard}"
    : "${MOUSE:=>}"
    : "${UP_SHORTCUTS:=true}"
    : "${SORT_DIR_TYPE:=name_asc}"
    : "${SORT_FILE_TYPE:=name_asc}"
    
    # Lista optimizada como string para búsqueda rápida (evita bucles)
    FILE_EXTENSIONS="|img|gzip|xz|tar|zip|gz|bz2|lz4|7z|rar|Z|"
    
    SHORTCUTS="DATA /data
    SDCARD /sdcard
    "
    
    identify_device
}

function identify_device() {
    [[ -n "$DYNAMIC" && -n "$A_B" ]] && return 0

    if [ -d "/dev/block/mapper" ]; then
        DYNAMIC="true"; GENERIC="false"; DEVICE_MAPPER="/dev/block/mapper"
    elif [ -d "/dev/block/by-name" ]; then
        DYNAMIC="false"; GENERIC="true"; DEVICE_MAPPER="/dev/block/by-name"
    else
        DEVICE_MAPPER="/dev/block/by-name"; GENERIC="true" # Fallback
    fi

    if ls "$DEVICE_MAPPER"/*_a >/dev/null 2>&1; then
        A_B="true"; A_O="false"
    else
        A_B="false"; A_O="true"
    fi
    
    # Definir variables de dispositivo para visualización
    DEVICE_NAME=$(getprop ro.product.system.device)
    DEVICE_MODEL=$(getprop ro.product.system.model)
    SOC=$(getprop ro.product.board)
}

function check_root_permissions() {
    [ "$(id -u)" -eq 0 ] && ROOT=true || ROOT=false
}

function exec_as_root() {
    if [ "$ROOT" = "false" ]; then
        if command -v tsu >/dev/null 2>&1; then tsu -c "$0" "$@"; exit 0; fi
        if command -v sudo >/dev/null 2>&1; then sudo "$0" "$@"; exit 0; fi
        if command -v su >/dev/null 2>&1; then su -c "sh $0 $@"; exit 0; fi
        echo "Root access is required."
        exit 1
    fi
}

# --- Funciones de Sistema de Archivos (Optimizadas) ---

function perform_resize_and_unshare_blocks() {
    local img="$1"
    echo "Optimizing image: $img"
    resize2fs "$img" 6G 2>$hider || echo "Warning: Fixed resize failed, trying auto..."
    echo "Fixing Errors..."
    e2fsck -fp "$img"
    echo "Unsharing blocks..."
    e2fsck -E unshare_blocks "$img"
    echo "Minimizing..."
    resize2fs -M "$img"
}

function undo_resize_and_unshare_blocks() {
    local img="$image_path"
    [ ! -f "$img" ] && return 1
    echo "Reverting/Repairing image..."
    e2fsck -f "$img"
    resize2fs -M "$img"
}

function mount_device() {
    local partition="$1"
    local mount_point="$2"
    mkdir -p "$mount_point"
    
    local loop_device=$(losetup -f 2>/dev/null)
    
    # Intento 1: Losetup
    if [ -n "$loop_device" ] && losetup "$loop_device" "$partition"; then
        local fs_type=$(blkid -o value -s TYPE "$loop_device")
        [ -z "$fs_type" ] && fs_type="ext4"
        
        if mount -t "$fs_type" -o rw,noatime "$loop_device" "$mount_point"; then
            echo "Mounted at $mount_point"
            ask # Pausa para que el usuario modifique
            umount "$mount_point"
            losetup -d "$loop_device"
            e2fsck -f "$partition"
            resize2fs -M "$partition"
            return 0
        fi
        losetup -d "$loop_device"
    fi
    
    # Intento 2: Montaje directo (Si el kernel lo soporta)
    if mount -o rw,noatime "$partition" "$mount_point"; then
        echo "Mounted directly at $mount_point"
        ask
        umount "$mount_point"
        e2fsck -f "$partition"
        resize2fs -M "$partition"
        return 0
    fi
    
    echo "Error: Mount failed."
    return 1
}

function extract_image_from_archive() {
    local archive="$1"
    local ext="${archive##*.}"
    local extracted_dir="$(dirname "$archive")/extracted"
    
    mkdir -p "$extracted_dir"
    echo "Extracting $archive..."

    case "${ext,,}" in
        zip) unzip -d "$extracted_dir" "$archive" >$hider 2>&1 ;;
        tgz|gz) tar -xzf "$archive" -C "$extracted_dir" >$hider 2>&1 ;;
        xz) xz -d -k "$archive" > "$extracted_dir/${archive%.*}" 2>$hider ;;
        tar) tar -xf "$archive" -C "$extracted_dir" >$hider 2>&1 ;;
        lz4) lz4 -d "$archive" "$extracted_dir/${archive%.*}" >$hider 2>&1 ;;
        *) echo "Generic extraction..."; 
           if command -v "extract_$ext" >/dev/null; then "extract_$ext" "$archive"; fi ;;
    esac
}

# --- Lógica Unificada (DRY - Don't Repeat Yourself) ---

function process_partitions() {
    local action="$1" # ota_rw, copy_only
    local partitions=("system" "vendor" "product" "odm")
    
    # Determinar sufijos
    local slots=("")
    [ "$A_B" = "true" ] && slots=("_a" "_b")
    local src_suffix=""
    [ "$A_B" = "true" ] && src_suffix="_a"

    ask

    for part in "${partitions[@]}"; do
        local var_name="CUSTOM_${part^^}_NAME"
        local custom_name="${!var_name}"
        
        local target_img="/sdcard/${part}.img"
        local source_dev="${DEVICE_MAPPER}/${custom_name}${src_suffix}"

        if [ -e "$source_dev" ]; then
            echo "Copying $source_dev -> $target_img"
            dd if="$source_dev" of="$target_img" bs=32M status=none
        else
            continue # Saltar si no existe partición
        fi

        perform_resize_and_unshare_blocks "$target_img"

        if [ "$action" = "ota_rw" ]; then
            if [ "$A_B" = "true" ]; then
                for slot in "${slots[@]}"; do
                    local flash_target="${DEVICE_MAPPER}/${custom_name}${slot}"
                    [ -e "$flash_target" ] && dd if="$target_img" of="$flash_target" bs=32M status=none
                done
            else
                dd if="$target_img" of="${DEVICE_MAPPER}/${custom_name}" bs=32M status=none
            fi
        fi
    done
}

# --- Configuración y TUI ---

function save_configurations() {
    local config_file="/cache/gsirw.conf"
    [ ! -w "/cache" ] && config_file="$HOME/gsirw.conf"

    cat <<EOF > "$config_file"
CUSTOM_SYSTEM_NAME="$CUSTOM_SYSTEM_NAME"
CUSTOM_VENDOR_NAME="$CUSTOM_VENDOR_NAME"
CUSTOM_PRODUCT_NAME="$CUSTOM_PRODUCT_NAME"
CUSTOM_ODM_NAME="$CUSTOM_ODM_NAME"
MOUSE="$MOUSE"
UP_SHORTCUTS="$UP_SHORTCUTS"
A_B="$A_B"
A_O="$A_O"
DYNAMIC="$DYNAMIC"
GENERIC="$GENERIC"
DEVICE_MAPPER="$DEVICE_MAPPER"
CUSTOM_MOUNT_POINT="$CUSTOM_MOUNT_POINT"
DEFAULT_START_POINT="$DEFAULT_START_POINT"
FILE_EXTENSIONS="$FILE_EXTENSIONS"
SHORTCUTS="$SHORTCUTS"
EOF
}

function load_saved_configurations() {
    local config_files=("/cache/gsirw.conf" "$HOME/gsirw.conf")
    for f in "${config_files[@]}"; do
        if [ -f "$f" ]; then source "$f"; break; fi
    done
}

function configure_manually() {
    clear_screen
    echo "Manual configuration not fully implemented in optimized version."
    echo "Edit $HOME/gsirw.conf manually or use basic settings."
    ask
}

# --- Explorador Interactivo (TUI Core) ---

function list_directories_and_files() {
    local dir="$1"
    local index=1
    local dirs=()
    local files=()
    
    shopt -s nullglob
    dirs=( "$dir"/*/ )
    files=( "$dir"/* )
    shopt -u nullglob

    if [ "$UP_SHORTCUTS" = "true" ]; then
        IFS=$'\n'
        for line in $SHORTCUTS; do
            local s_name=$(echo "$line" | awk '{print $1}')
            local s_path=$(echo "$line" | awk '{print $2}')
            if [ -n "$s_name" ]; then echo "$index~[S]~$s_name~$s_path"; ((index++)); fi
        done
        unset IFS
    fi

    for d in "${dirs[@]}"; do
        echo "$index~[D]~$(basename "$d")/~"
        ((index++))
    done

    for f in "${files[@]}"; do
        local f_name=$(basename "$f")
        local ext="${f_name##*.}"
        if [[ "$FILE_EXTENSIONS" == *"|${ext,,}|"* ]]; then
            echo "$index~[F]~$f_name~"
            ((index++))
        fi
    done
}

function interactive_core() {
    export mode="$1"
    local selected_index=1
    local image_path="$DEFAULT_START_POINT"
    [ -z "$image_path" ] && image_path="/sdcard"

    local needs_refresh=true
    local list_output=""
    local total_items=0

    while true; do
        if [ "$needs_refresh" = true ]; then
            list_output=$(list_directories_and_files "$image_path")
            total_items=$(echo "$list_output" | wc -l)
            [ -z "$list_output" ] && total_items=0
            needs_refresh=false
        fi

        printf '\033[H\033[J'
        echo "PATH: $image_path"
        echo "------------------------------------------------"
        
        if [ "$total_items" -eq 0 ]; then
            echo "  (Empty)"
        else
            echo "$list_output" | awk -v sel="$selected_index" -v ptr="$MOUSE" '
            {
                split($0, arr, "~");
                name = arr[3];
                if (NR == sel) printf "%s %s %s\n", ptr, arr[2], name;
                else           printf "  %s %s\n", arr[2], name;
            }'
        fi

        read -rsn1 input
        if [[ "$input" == $'\x1b' ]]; then
            read -rsn2 -t 0.1 input
            case "$input" in '[A') input="w";; '[B') input="s";; '[C') input="d";; '[D') input="a";; esac
        fi

        case "${input,,}" in
            q) break ;;
            w) ((selected_index--)); [ $selected_index -lt 1 ] && selected_index=1 ;;
            s) ((selected_index++)); [ $selected_index -gt $total_items ] && selected_index=$total_items ;;
            a) image_path=$(dirname "$image_path"); selected_index=1; needs_refresh=true ;;
            d)
                local item=$(echo "$list_output" | sed -n "${selected_index}p")
                local type=$(echo "$item" | cut -d '~' -f 2)
                local val=$(echo "$item" | cut -d '~' -f 3)
                
                if [[ "$type" == "[D]" ]]; then
                    image_path="${image_path%/}/$val"
                    selected_index=1; needs_refresh=true
                elif [[ "$type" == "[S]" ]]; then
                    image_path=$(echo "$item" | cut -d '~' -f 4)
                    selected_index=1; needs_refresh=true
                elif [[ "$type" == "[F]" ]]; then
                    select_file="$val"; export image_path="${image_path%/}"; right_logic; needs_refresh=true
                fi
                ;;
        esac
    done
}

function right_logic() {
    local full_path="$image_path/$select_file"
    local ext="${select_file##*.}"
    
    if [[ "$ext" == "img" ]]; then
        ask
        case "$mode" in
            "modification"|"user_modification"|"process_user_selected_image")
                perform_resize_and_unshare_blocks "$full_path"
                [ "$mode" != "process_user_selected_image" ] && mount_device "$full_path" "$CUSTOM_MOUNT_POINT"
                ;;
            "undo") undo_resize_and_unshare_blocks ;;
            "flash") echo "Flash logic here..." ;;
        esac
    else
        ask
        extract_image_from_archive "$full_path"
    fi
}

# --- Extras (Prank Restored) ---

function simulate_bootloader_patch() {
    echo -e "${NC}Generating specialized patch keys for $DEVICE_MODEL${NC}"
    echo -e "${NC}Modifying kernel for library adjustment${NC}"
    sleep 2
    echo -e "${BLUE}Attempting to remove security patches from $DEVICE_NAME's Bootloader"
    echo -e "${GREEN}Removal successful."
    sleep 1
}

function prank() {
    clear_screen
    local width=$(get_term_width)
    local bar=$(printf '%*s' "$width" | tr ' ' '-')
    
    echo -e "${GREEN}Initializing attempt to remove Bootloader security patches on ${DEVICE_NAME}...${NC}"
    simulate_bootloader_patch
    echo ""
    echo -e "${RED}$bar"
    echo -e "${RED}ERROR: A critical problem has been detected.${NC}"
    echo -e "${RED}Please do not turn off the device.${NC}"
    echo -e "${RED}$bar"
    echo ""
    echo -e "${RED}Checking SELinux integrity... Failed.${NC}"
    echo "Press any key to repair..."
    read -rsn1
    
    # Fake glitch
    center_ansi "$LOGOV2"
    sleep 3
    echo "Rebooting..."
    sleep 1
    clear_screen
}

# --- Menú Principal (Interfaz Original Restaurada) ---

function GSIRW() {
    clear_screen
    stty -echo
    progressive_print "$(print_justified "Initialized the great wonder created by Dark")" 0.02
    
    sleepy 1
    clear_screen
    progressive_print "$(print_justified "Identifying device parameters in case of emergency")" 0.02
    identify_device
    
    sleepy 1
    clear_screen
    progressive_print "$(print_justified "Loading secretly stored settings")" 0.02
    load_saved_configurations
    
    sleepy 1
    clear_screen
    
    # Mostrar LOGO centrado
    center_ansi "$LOGO"
    
    progressive_print "$(print_justified "AIO (ALL IN ONE) GSI / OTA  -RW  ")" 0.1
    sleepy 1
    echo ""
    print_justified "Select an option:"
    sleepy 0.5
    print_justified "1.Process the user-selected image."
    print_justified "2.Process a copy of the installed system image."
    print_justified "3.Process to return the ota in rw."
    print_justified "4.Process and modify image."
    print_justified "5.Modify image."
    print_justified "6.Undo Any Modification."
    print_justified "7.Manually configure options."
    print_justified "8.Super Secret Settings ($SOC)"
    print_justified "0.Exit GSI-rw."
    sleepy 0.5
    
    echo -n "$(print_justified "Choose an option: ")"
    stty echo
    read -rn1 option
    echo ""

    case $option in
        1) interactive_core "process_user_selected_image" ;;
        2) process_partitions "copy_only" ;;
        3) process_partitions "ota_rw" ;;
        4) interactive_core "user_modification" ;;
        5) interactive_core "modification" ;;
        6) interactive_core "undo" ;;
        7) configure_manually ;;
        8) prank ;;
        0) 
            progressive_print "$(print_justified "Why did you want to enter the GSIRW if you are ultimately going to leave?")" 0.05
            exit ;;
        *) GSIRW ;;
    esac
}

# --- MAIN ENTRY POINT (Argument Handling Restored) ---

function MAIN() {
    # Manejo de animación
    ANIMATION=false
    if [[ "$1" == "--animation" || "$1" == "-a" ]]; then
        ANIMATION=true
        shift
    fi

    default
    
    # Determinar modo por nombre del script o argumento
    local call_name="$(basename "$0")"
    
    case "$call_name" in
        "gsirw_proces_image") interactive_core "process_user_selected_image" ;;
        "gsirw_sys_installed") process_partitions "copy_only" ;;
        "gsirw_ota_rw") process_partitions "ota_rw" ;;
        "gsirw_mount") interactive_core "modification" ;;
        "gsirw_undo") interactive_core "undo" ;;
        "gsirw") GSIRW ;;
        *) 
            # Si se llama con argumentos directos
            if [ -n "$1" ]; then
                case "$1" in
                    "flash") interactive_core "flash" ;;
                    *) GSIRW ;;
                esac
            else
                GSIRW
            fi
            ;;
    esac
}

function CHECK_ROOT_AND_DEPENDENCIES() {
    # Check eficiente de dependencias para Termux
    if [ -n "$TERMUX_VERSION" ]; then
        local missing=()
        for pkg in tsu android-tools file e2fsprogs zip unzip lz4 tar xz-utils; do
            if ! dpkg -s "$pkg" >/dev/null 2>&1; then missing+=("$pkg"); fi
        done
        if [ ${#missing[@]} -gt 0 ]; then
            echo "Installing dependencies..."
            apt update -y && apt install -y "${missing[@]}"
        fi
    fi

    check_root_permissions
    exec_as_root "$@"
}

# Iniciar
CHECK_ROOT_AND_DEPENDENCIES "$@"
MAIN "$@
